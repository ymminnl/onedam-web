# Custom Images

Place your custom images here.

-   For the Hero section background, name your image `hero-bg.jpg`.
-   For news articles, use names like `news-default-1.jpg`, `news-default-2.jpg`, etc., as referenced in `src/data/news.json`.

These images will be served directly by the web server.